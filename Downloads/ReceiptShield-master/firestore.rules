rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow all operations for testing - we can add security later
    match /{document=**} {
      allow read, write: if true;
    }
  }
}